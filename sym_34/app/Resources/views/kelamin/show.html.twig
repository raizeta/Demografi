{% extends 'base.html.twig' %}

{% block body %}
    <h1>Kelamin</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ kelamin.id }}</td>
            </tr>
            <tr>
                <th>Namakelamin</th>
                <td>{{ kelamin.namaKelamin }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('kelamin_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('kelamin_edit', { 'id': kelamin.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
